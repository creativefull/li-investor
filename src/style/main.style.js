import {
    RkTheme
} from 'react-native-ui-kitten'

RkTheme.setType('RkCard', 'panel', {
    marginBottom: 20,
})

RkTheme.setType('RkButton', 'listBtn', {
    marginBottom : 10
})

RkTheme.setType('RkButton', 'defaultBtn', {
    padding: 10,
    backgroundColor: '#FEFEFE'
})

RkTheme.setTheme('RkText', 'textBtn', {
    margin : 10
})